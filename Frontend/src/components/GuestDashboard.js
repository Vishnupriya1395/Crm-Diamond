import React from 'react';

const GuestDashboard = () => {
  // Fetch and display forms for the logged-in guest user
  return <div>Guest Dashboard - Limited access</div>;
};

export default GuestDashboard;
